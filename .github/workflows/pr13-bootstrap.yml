name: PR 13 bootstrap

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'refactor/background-task-coordinator'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply generated refactors
        run: |
          python scripts/pr13_patch_service.py
          python scripts/pr13_patch_gui.py
      - name: Validate syntax
        run: >-
          python -m compileall -q
          src/tutor_assistant/content
          src/tutor_assistant/ui/background.py
          src/tutor_assistant/ui/background_tasks.py
          src/tutor_assistant/ui/concurrent_app.py
      - name: Commit generated changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/tutor_assistant/content/service.py src/tutor_assistant/ui/concurrent_app.py
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Migrate GUI background operations to coordinator"
          git push origin HEAD:${{ github.head_ref }}
